import ReactLoading from "react-loading";

const Loading = () => {
  return (
    <ReactLoading
      type="spinningBubbles"
      color="#1890ff"
      height={100}
      width={100}
    />
  );
};

export default Loading;
